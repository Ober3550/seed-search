# Ground-truth dumps

These are generated by running `verifier/verify/verify-seed.sh <seed>` which
produces a real game universe dump via Factorio headless + RCON.

Regenerate with:
```sh
verifier/verify/verify-seed.sh <seed>
```

These files are gitignored.
